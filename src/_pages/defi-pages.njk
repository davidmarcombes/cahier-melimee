---
pagination:
  data: collections.defisMeta
  size: 1
  alias: s
layout: timed-player
tags: []
lang: fr
permalink: "/fr/defis/{{ s.id }}/"
eleventyComputed:
  series: "{{ s.series }}"
  title: "{{ s.title }}"
  folder: "defis"
  level: "{{ s.level }}"
  topic: "{{ s.topic }}"
  subtopic: "{{ s.subtopic }}"
  difficulty: "{{ s.difficulty }}"
  duration: "{{ s.duration }}"
  id: "{{ s.id }}"
---
