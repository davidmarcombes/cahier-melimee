{# Fraction Representation #}
<template x-if="cur.type === 'fraction' && cur.fraction">
  <div class="flex flex-col items-center mb-8">
    <div class="flex flex-wrap justify-center gap-6 mb-4">
      <template x-for="shape in (fractionShapes || [])" :key="shape.idx">
        <div class="relative w-32 h-32 sm:w-40 sm:h-40" x-html="shape.svg">
        </div>
      </template>
    </div>

    {# Instructions from body #}
    <div class="mt-4 prose dark:prose-invert text-base text-center mb-4 max-w-none italic text-content-subtle" x-html="cur.body"></div>
  </div>
</template>
